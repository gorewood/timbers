# Timbers environment configuration
#
# Resolution order (first match wins, env vars always take precedence):
#   1. $CWD/.env.local           <- per-repo override (copy this file)
#   2. $CWD/.env                 <- per-repo
#   3. ~/.config/timbers/env     <- global (set once, works in all repos)
#
# For per-repo: cp .env.example .env.local
# For global:   mkdir -p ~/.config/timbers && cp .env.example ~/.config/timbers/env
#
# .env and .env.local are gitignored. Edit and uncomment the keys you need.

# Required for Anthropic models (haiku, sonnet, opus)
# ANTHROPIC_API_KEY=sk-ant-...

# Required for OpenAI models (nano, mini, gpt-5)
# OPENAI_API_KEY=sk-...

# Required for Google models (flash, pro)
# GOOGLE_API_KEY=...

# Local LLM server URL (default: http://localhost:1234/v1)
# LOCAL_LLM_URL=http://localhost:1234/v1
